- hosts: webserver
  user: vagrant

  roles: 
    - server
    - apache2
    - php
    - mysql
    - wordpress

- hosts: logserver
  user: vagrant

  roles: 
    - logserver

- hosts: moniserv
  user: vagrant

  roles: 
    - moniserv
