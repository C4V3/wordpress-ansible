- hosts: webserver
  become: yes
  become_method: sudo
  roles: 
    - server
    - apache2
    - php
    - mysql
    - wordpress

- hosts: logserver

  roles: 
    - logserver

- hosts: moniserv

  roles: 
    - moniserv
